/* autogenerated by Processing revision 1293 on 2024-11-04 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class stu7 extends PApplet {

penguin pg,pp;

public void setup() {
  /* size commented out by preprocessor */;
  // 초기 변수
  pg = new penguin(2,3);
  pp = new penguin(3,2);
}

public void draw() {
  background(255);
  pg.move();
  pg.show();
  pp.move();
  pp.show();
}

class penguin {
  // 생성자
  penguin(float ax, float ay) {
    x = 100;
    y = 150;
    d = 100;
    vx = ax;
    vy = ay;
    
  }
  
  // 멤버 데이터
  float x, y, d, vx,vy;

  // 멤버 함수
  public void show() {
    // 펭귄 몸통
    stroke(0);
    strokeWeight(d * 0.06f);
    fill(255); // 흰색
    ellipse(x, y, d, d * 1.5f); // 몸통
    strokeWeight(1);
    
    // 펭귄 머리
    fill(0); // 검은색
    ellipse(x, y - d * 0.5f, d * 0.75f, d * 0.75f); // 머리
    
    // 눈
    stroke(255);
    strokeWeight(d * 0.0275f);
    fill(0); // 검은색
    ellipse(x - d * 0.15f, y - d * 0.6f, d * 0.1f, d * 0.1f); // 왼쪽 눈
    ellipse(x + d * 0.15f, y - d * 0.6f, d * 0.1f, d * 0.1f); // 오른쪽 눈
    strokeWeight(1);
    
    // 부리
    fill(255, 165, 0); // 주황색
    stroke(0);
    beginShape();
    vertex(x - d * 0.1f, y - d * 0.4f);
    vertex(x, y - d * 0.35f);
    vertex(x + d * 0.1f, y - d * 0.4f); 
    vertex(x, y - d * 0.45f); 
    vertex(x - d * 0.1f, y - d * 0.4f);
    vertex(x + d * 0.1f, y - d * 0.4f); 
    endShape(CLOSE);
    
    // 왼쪽 팔 (사선)
    fill(0); // 검은색
    beginShape();
    vertex(x - d * 0.4f, y - d * 0.2f); // 팔 시작점
    vertex(x - d * 0.8f, y + d * 0.3f); // 팔 끝점
    vertex(x - d * 0.55f, y - d * 0.2f); // 팔 끝점
    endShape(CLOSE);
    
    // 오른쪽 팔 (사선)
    fill(0); // 검은색
    beginShape();
    vertex(x + d * 0.4f, y - d * 0.2f); // 팔 시작점
    vertex(x + d * 0.8f, y + d * 0.3f); // 팔 끝점
    vertex(x + d * 0.55f, y - d * 0.2f); // 팔 끝점
    endShape(CLOSE);
    
    // 발
    fill(0); // 검은색
    ellipse(x - d * 0.2f, y + d * 0.8f, d * 0.4f, d * 0.2f); // 왼쪽 발
    ellipse(x + d * 0.2f, y + d * 0.8f, d * 0.4f, d * 0.2f); // 오른쪽 발
  }
  
  public void move() {
    x += vx;
    y += vy;

    // 경계 처리
    if (x > width || x < 0) {
      vx = -vx;
    } 
    if (y > height || y < 0) {
      vy = -vy;
    }
  }
}


  public void settings() { size(700, 700); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "stu7" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
